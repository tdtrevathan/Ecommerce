import { ItemDetailsService } from '@/services/itemDetailsService'

describe('ItemDetialsService', () => {
  test('get item details, provide non existing guild, returns ', () => {
    const itemDetailsService = new ItemDetailsService();

  });
});
